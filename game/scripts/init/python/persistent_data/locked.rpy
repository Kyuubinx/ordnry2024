init python:

    if persistent.definitions == None:
        persistent.definitions = False
    if persistent.unlockch2 == None:
        persistent.unlockch2 = False
    if persistent.unlockch3 == None:
        persistent.unlockch3 = False
