init python:

    if persistent.unl_amy == None:
        persistent.unl_amy = False
    if persistent.encbierce1 == None:
        persistent.encbierce1 = False
    if persistent.encpaige1 == None:
        persistent.encpaige1 = False
    if persistent.unl_charlie == None:
        persistent.unl_charlie = False
    if persistent.encleo1 == None:
        persistent.encleo1 = False
    if persistent.encmarie1 == None:
        persistent.encmarie1 = False
    if persistent.encroxy1 == None:
        persistent.encroxy1

    encamy1 = True
    encpaige1 = True
